# 개발 계획

기준일: 2026-09-12. 이번 문서 작성으로 아래 기능을 구현하거나 배포하지는 않았다.
목표는 검증된 원목 획득 스킬을 출발점으로, 호출 예산 안에서 서바이벌 목표를 선택하고 수행하는 에이전트다.

## 현재 출발점

Fabric 1.21.1 관측·동작, 별도 Docker 브리지와 Python 에이전트, Astra 단일 계획 호출,
최대 3행동/60초의 모델 없는 wood 스킬이 있다. 실제 월드에서 접근→채굴→획득을 확인했다.
단거리 평지 외 경로, 제작, 영속 스킬 라이브러리, 자동 커리큘럼은 아직 없다.

## 우선순위와 완료 기준

| 순서 | 작업 | 완료 기준 | 의존성 |
|---|---|---|---|
| P0 | 전체 소스의 버전 관리 구조 확정 | 새 환경에서 두 구성요소의 정확한 버전을 확보하고 빌드 가능 | 없음 |
| P1 | 취소·실행 수명주기와 회귀 검증 | 진행 중 작업 취소 확인, 재연결 중복 실행 방지, 실패 조건별 테스트 통과 | P0 |
| P2 | 결정적 스킬 라이브러리 | wood를 등록·조회·실행하고 선행/종료 조건과 버전을 기록 | P1 |
| P3 | 제한된 이동·재탐색 | 관측 범위 내 대체 후보/경로를 예산 안에서 선택, 정체 시 종료 | P1, P2 |
| P4 | 인벤토리·제작·장비 스킬 | 판자→작업대→나무 도구를 재료 소비와 산출물로 검증 | P2 |
| P5 | 예산이 있는 Astra 목표 선택 | 허용된 스킬만 선택하고 호출·토큰·시간 상한에서 중단 | P2, P3, P4 |
| P6 | 커리큘럼과 실패 피드백 | 여러 목표의 결과로 다음 목표를 선택하고 재현 가능한 평가 수행 | P5 |

### P0. 소스 보존과 재현성

현재 Docker 폴더는 Fabric 저장소 밖이고 별도 Git 저장소도 아니다. Fabric Push만으로는
Python/Node/Docker 구현이 보존되지 않는다. 다음 두 방식 중 하나를 결정한다.

- 권장: 배포용 Docker 소스를 저장소의 `docker/`에 포함하고, 실제 .env·볼륨·로그는 외부 실행 폴더에 유지.
- 대안: Docker를 별도 저장소로 관리하고 Fabric/프로토콜 호환 버전을 양쪽에 명시.

실행 중인 폴더를 무작정 이동하지 않고 소스·설정 템플릿·런타임 데이터를 분리한다.
추적 대상은 Dockerfile, compose, 소스, 테스트, lockfile, 설정 템플릿, 실행 도구다.
완료 조건은 깨끗한 clone에서 문서만으로 빌드·설정 생성·모델 없는 연결 시험까지 재현되는 것이다.
Java 빌드와 Docker 단위 테스트를 CI에 추가하되 실제 API 키·월드·유료 호출은 CI에 넣지 않는다.

### P1. 중단과 검증을 먼저 안정화

현재 stop은 단일 실행 큐를 선점하지 못한다. 별도 취소 경로와 action ID 기반 취소 확인을 설계한다.
목표 취소→현재 행동 취소→입력 해제→최종 상태 확인을 한 흐름으로 기록한다.
타임아웃 후 stop 요청이 큐에 들어갔다는 사실만으로 정지 완료를 선언하지 않는다.

메뉴·죽음·차원 전환·연결 단절·요청 중복·오래된 관측·인벤토리 동기화 지연을 테스트한다.
실게임에서는 벽 접촉, 직각 회전, 막힌 경로, 단차·물, 움직이는 드롭을 따로 재현한다.
공개 회귀 자료에는 합성 관측과 최소 재현 조건만 넣고 실제 월드 로그는 로컬에 보관한다.
다음 기능으로 넘어갈 기준은 취소 확인과 실패 이유가 일관되고, 동일 실패가 자동 테스트로 잡히는 것이다.

### P2. 검증된 스킬을 데이터로 관리

스킬에 ID·버전·입력 스키마·필요 capability·선행 조건·행동/시간 예산·성공 판정 함수를 둔다.
wood를 첫 등록 스킬로 옮기되 실행 동작과 성공 기준을 유지한다.
실행 기록에는 스킬 버전, 종료 이유, 인벤토리 변화, 관측 스키마 버전을 저장한다.
처음에는 명시적 ID 조회와 조건 필터로 충분하다. 임베딩 검색은 스킬 수와 검색 실패가 늘 때 도입한다.
완료 조건은 같은 버전의 스킬을 반복 실행하고, 코드 생성 없이 결과를 비교할 수 있는 것이다.

### P3. 이동 범위 확장

먼저 가까운 목표 선택 실패를 분류하고 대체 후보를 제한 횟수 안에서 선택한다.
정체 복구와 드롭 위치 재관측에도 각각 예산을 부여한다. 다음으로 높이 1블록 이동을 별도 행동으로 검증한다.
물·낙하·미로드 공간은 계속 구분하고, 실패를 없애기 위해 충돌·지지 검사를 끄지 않는다.
장거리 탐색은 구간별 재관측과 돌아갈 위치 기록이 확보된 다음 추가한다.

### P4. 첫 제작 목표

인벤토리 슬롯 선택과 도구 장착, 제한된 레시피 제작, 작업대 설치/상호작용을 순서대로 추가한다.
바닐라 상호작용을 사용하고 서버가 반영한 재료 감소와 산출물 증가로 성공을 판정한다.
재료 부족·공간 부족·화면 종료·도구 변경은 실패로 기록하고 무한 재시도하지 않는다.
첫 완료 목표는 원목 획득→판자→작업대→나무 도구이며 이후 돌 도구로 확장한다.

### P5. Astra를 실행기 위의 선택기로 사용

모델은 관측 요약과 등록된 스킬 목록에서 다음 목표/스킬을 선택한다.
모델 출력은 구조화 스키마로 검증하고 실제 행동은 기존 결정적 실행기를 통과한다.
모델 호출 수, 출력 토큰, 총 시간, 행동 수, 실패 재시도 수에 상한을 둔다.
사용량을 수집하고 비용 추정과 실제 사용량을 구분한다. 무제한 유료 루프를 기본값으로 두지 않는다.
목표 성공 판정은 모델의 자연어 평가가 아니라 상태 비교와 스킬 검증기로 수행한다.

### P6. Voyager 방식 확장

스킬 성공률·선행 조건·최근 실패를 바탕으로 달성 가능한 다음 목표를 선택한다.
실패 피드백은 버전이 있는 스킬 수정 제안으로 만들고 회귀 테스트 후 채택한다.
임의 생성 코드를 도입한다면 호스트 파일·키·네트워크 접근을 분리한 별도 실행 환경을 먼저 마련한다.
평가에는 고정된 재현 조건, 시도 수, 성공률, 평균 행동/호출 수, 사망·중단 횟수를 기록한다.
한 월드의 한 번 성공을 일반적인 생존 능력으로 해석하지 않는다.

## 다음 한 번의 작업 범위

P0에서 소스 배치 방식을 확정하고 두 구성요소를 새 환경에서 재현할 수 있도록 정리한다.
그다음 P1의 실행 중 취소 프로토콜을 구현한다. 제작과 유료 자동 반복은 그 이후로 둔다.
각 작업은 코드·검증·사용법·제약을 함께 갱신하고 독립적으로 검토 가능한 커밋 단위로 마무리한다.
